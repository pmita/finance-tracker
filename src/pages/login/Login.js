import React from 'react';
import styles from './Login.module.css';

const Login = () => {
    return(
        <div>
            <h1>Hello from Login Page component</h1>
        </div>
    );
}

export default Login;